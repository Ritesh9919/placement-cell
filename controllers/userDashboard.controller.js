import {User} from '../models/user.model.js';
import {ApiError} from '../utils/apiError.js';
import {ApiResponse} from '../utils/apiResponse.js'




export const getEligibleJobs = async(req, res, next)=> {
    try {
        
    } catch (error) {
        console.error(error);
        next(error);
    }
}


export const getAppliedJobs = async(req, res, next)=> {
    try {
        
    } catch (error) {
        console.error(error);
        next(error);
    }
}


export const getRejectedJobs = async(req, res, next)=> {
    try {
        
    } catch (error) {
        console.error(error);
        next(error);
    }
}


export const getHiredJobs = async(req, res, next)=> {
    try {
        
    } catch (error) {
        console.error(error);
        next(error);
    }
}